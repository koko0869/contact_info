import React from "react";

const insertButton = ({ infos}) => {
  console.log("테스트");
  console.log(infos);
  return (
    <div>
      <button>추가하기</button>
    </div>
  );
};

export default insertButton;
